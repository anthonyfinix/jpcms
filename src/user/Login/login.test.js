import { shallow } from 'enzyme'
import Login from './index';
describe("Login", () => {
    it("Show have two field", () => { })
    it("Redirect to root if logged in", () => { })
    it("Should redirect to root if login successful", () => { })
})